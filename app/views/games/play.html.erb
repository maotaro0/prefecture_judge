<main class="game-page">
  <section class="game-container">
    <h1 class="game-title">都道府県ジャッジ</h1>

    <p class="game-description">
      正解だと思う3つの都道府県を選んでください。
    </p>

    <div class="hint-area">
     <p class="hint-title">ヒント</p>

     <div class="hint-list">
       <% @displayed_hints.each do |hint| %>
        <p class="hint-text">
          <%= hint[:text] %> → <%= hint[:count] %> / 3
        </p>
       <% end %>
      </div>

      <% if @displayed_hints.length < @hints.length %>
        <%= link_to "次のヒント", request.path, class: "hint-button" %>
      <% end %>
   </div>

    <%= form_with url: answer_game_path, method: :post, local: true do %>
       <p class="selection-count">
        選択中：<span id="selected-count">0</span> / 3
       </p>
       <p class="selection-message" id="selection-message"></p>


      <div class="prefecture-list">
        <% @candidate_prefectures.each do |prefecture| %>
          <button
            class="prefecture-card"
            type="button"
            data-prefecture-id="<%= prefecture.id %>"
          >
            <%= prefecture.name %>
          </button>
        <% end %>
      </div>

      <input
        type="hidden"
        name="selected_prefecture_ids"
        id="selected-prefecture-ids"
        value=""
      >

      <button type="submit" class="answer-button" disabled>
        回答する
      </button>
    <% end %>
  </section>
</main>